# Render.com deployment configuration
# This file ensures Render uses the correct buildpack for Java

# Java version (required for Java buildpack)
java.runtime.version=17

# Maven wrapper (if available) or system Maven
maven.version=3.8.6